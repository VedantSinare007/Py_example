# Py_example

# 💰 Expense Tracker (Python Project)

## 📌 Overview
The **Expense Tracker** is a command-line Python application that helps users manage and monitor their daily expenses. It allows users to add, view, update, delete, and search expenses efficiently, storing all data in an SQLite database for persistence.

## 🎯 Features
- Add new expense records (category, amount, date, note)
- View all expenses in a tabular format
- Search expenses by category or date
- View total expenses and monthly summaries
- Update or delete specific entries
- Data stored persistently using SQLite3

## 🧠 Technologies Used
- Python 3
- SQLite3 (built-in database)
- Tabulate (for clean table display)

## 🚀 How to Run
1. Clone the repository  
   ```bash
   git clone https://github.com/<your-username>/Expense-Tracker.git
   cd Expense-Tracker
